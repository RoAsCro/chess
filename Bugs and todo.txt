Less urgent:
Bug?: two variables named checking, change one of them
Fix: Simplify the checkCheck method so it works like checkCheckmate
	Make a general method for iterating through directions?
Reduce number of steps in pawn checkmate checking
simplify the input checking with ? : if else (Probs also some Null checks?)
generlise more things to pawnCheck
run more checkmate checks 
-en passant
-castling



TO CHECK
PASSED check taking works correctly in terms of moving a piece onto an occupied space - colour check has changed
PASSED Check the new beTaken method for Piece works
PASSED check Castling still works, updated selectedPiece.y to startY in the rook movement code
New CheckType method - Looks to be working

Implement:
3 Move repetition
Promotion
